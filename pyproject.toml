[project]
name = "walmart-dbt-fabric"
version = "0.1.0"
description = "dbt transformations for a Walmart-style retail warehouse on Microsoft Fabric"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "dbt-core>=1.11.12",
    "dbt-fabric>=1.10.0",
    "python-dotenv>=1.0.0",
]
